package io.binarycodes.calculators.loan.domain;

/** How often a recurring extra prepayment is made on top of the regular EMI. */
public enum PrepaymentFrequency {
    MONTHLY,
    QUARTERLY,
    YEARLY
}
